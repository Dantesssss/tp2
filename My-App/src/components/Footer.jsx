import React from 'react';
import '../style/footerStyle.css'; // Importa el archivo CSS

const Footer = ({ email }) => {
  return (
    <footer>
      <section>
        <a href="#titulo">Ir al comienzo</a>
      </section>
      <p>ZOCANETA</p>
    </footer>
  );
};

export default Footer;
